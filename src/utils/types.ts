export type Session = {
  id?: string;
  firstName: string;
  lastName: string;
  email: string;
  date: string;
};

export type Message = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  message: string;
  date: string;
};
